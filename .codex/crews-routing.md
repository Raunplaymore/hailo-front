# Codex Agent Crews Routing

This file is generated by `setup-codex-agent-crews`.

## Required Context Load

For coding tasks in this project, read these files before editing:

1. `.codex/crews-config.md`
2. `.codex/stack-profile.md`
3. `AGENTS.md`, if present
4. `.codex/01-golf-analysis-architecture.md`
5. `.codex/02-cross-repo-refactor-plan.md`
6. `.codex/03-body-club-fusion-schema.md`
7. `docs/ai/`, if present and relevant

If `.codex/stack-profile.md` starts with `# Stack Profile: Monorepo Index`, determine the workspace first:

1. Use an explicit workspace named by the user.
2. Otherwise infer from file paths under the workspace matrix.
3. If neither is possible, ask which workspace to use.

## Related Project Workspace

This project is one part of a Raspberry Pi + Hailo golf swing capture and analysis workspace.
Before changing API contracts, camera/inference flows, shared configuration, deployment scripts, calibration data, or integration behavior, inspect the relevant sibling projects below.

| Project | Path | Role | Check before changing |
|---|---|---|---|
| `pi_web` | `/Users/hwangjunguk/Desktop/dir_UK/dir_sandbox/pi_web` | Vite React frontend for swing capture, live preview overlay, session history, upload, and analysis results. | UI behavior, browser API calls, Vite env keys, frontend proxy behavior, displayed analysis schema. |
| `pi_service` | `/Users/hwangjunguk/Desktop/dir_UK/dir_sandbox/pi_service` | Node/Express backend (`hailo-back`) for uploads, static file serving, analysis job orchestration, and calls to `hailo-infer`. | `/api/analyze/*`, `/api/files/*`, upload/data paths, `INFER_BASE_URL`, analysis status/result schema. |
| `pi_camera` | `/Users/hwangjunguk/Desktop/dir_UK/dir_sandbox/pi_camera` | Raspberry Pi camera capture server for rpicam/libcamera preview, capture, session recording, Hailo metadata generation, and analysis trigger calls. | `/api/camera/*`, `/api/session/*`, MJPEG streams, capture/session file naming, meta JSON format, `ANALYZE_URL`, camera env vars. |
| `hailo-infer` | `/Users/hwangjunguk/Desktop/dir_UK/dir_sandbox/hailo-infer` | Python inference/coach microservice that consumes Hailo metadata, segments swing events, computes coaching metrics, and writes analysis JSON. | `/v1/jobs`, job/result schema, meta-driven analysis logic, `UPLOAD_DIR`/`META_DIR`/`DATA_DIR`, systemd/deploy behavior. |
| `pi_charuco` | `/Users/hwangjunguk/Desktop/dir_UK/dir_sandbox/pi_charuco` | ChArUco calibration helper with captured calibration images and generated camera intrinsics/FOV JSON. | Lens calibration values, FOV/intrinsics assumptions, calibration JSON copied into camera/frontend/backend config. |

## Golf Analyzer Baseline

This workspace is no longer treated as a single web app. The default operating model is:

- `pi_web` as workspace anchor
- `pi_service`, `pi_camera`, `hailo-infer` inspected together for API, inference, or analysis changes
- body analysis handled by pose-based tooling
- Hailo path biased toward club-only inference

Any Codex run that touches analysis behavior should validate against the three `.codex/0*-*.md`
documents before changing code.

## Role Guides

Use the plugin role references as needed:

- `references/roles/planner.md` for requirements, architecture, and task breakdown.
- `references/roles/component-dev.md` for UI, screens, components, and styling.
- `references/roles/api-integrator.md` for APIs, data layer, types, stores, and services.
- `references/roles/reviewer.md` for final review and stack-profile based checks.

Do not force delegation for every edit. Codex may edit directly when the change is clear, scoped, and consistent with the loaded project context. Use subagents only when the user asks for delegation/parallel work or when the task has genuinely independent slices.

## Workflow Routing

- New feature or page: use `$codex-new-feature`.
- Existing bug or broken behavior: use `$codex-bugfix`.
- Deploy or release: use `$codex-deploy`.
- New project-specific roles or new stack domains: use `$codex-harness`.
- Remove generated files: use `$remove-codex-agent-crews`.

## Quick Plan

Before non-trivial code edits, state:

```text
Quick Plan
- Target files:
- Change:
- Validation:
- Risk:
```

Skip this only for direct answers or tiny read-only checks.
